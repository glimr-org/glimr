//// Submission Model (GENERATED - DO NOT EDIT)
////
//// Generated by glimr/db/gen

import gleam/dynamic/decode
import glimr/db/pool.{type Pool}
import glimr/db/pool_connection.{type DbError, type PoolConnection, NotFound}
import glimr/db/query

pub type Submission {
  Submission(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

pub fn decoder() -> decode.Decoder(Submission) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(Submission(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn delete(pool pool: Pool, id id: Int) -> Result(Int, DbError) {
  case pool.checkout(pool) {
    Ok(connection) -> {
      let result = delete_wc(connection: connection, id: id)
      pool.checkin(pool, connection)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn delete_wc(
  connection connection: PoolConnection,
  id id: Int,
) -> Result(Int, DbError) {
  query.execute(connection, "DELETE FROM submissions WHERE id = $1", [
    pool_connection.int(id),
  ])
}

pub type FindRow {
  FindRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn find_row_decoder() -> decode.Decoder(FindRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(FindRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn find(pool pool: Pool, id id: Int) -> Result(FindRow, DbError) {
  case pool.checkout(pool) {
    Ok(connection) -> {
      let result = find_wc(connection: connection, id: id)
      pool.checkin(pool, connection)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_wc(
  connection connection: PoolConnection,
  id id: Int,
) -> Result(FindRow, DbError) {
  case
    query.select_all(
      connection,
      "SELECT * FROM submissions WHERE id = $1",
      [pool_connection.int(id)],
      find_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(pool_connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type ListAllRow {
  ListAllRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn list_all_row_decoder() -> decode.Decoder(ListAllRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(ListAllRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn list_all(pool pool: Pool) -> Result(List(ListAllRow), DbError) {
  case pool.checkout(pool) {
    Ok(connection) -> {
      let result = list_all_wc(connection: connection)
      pool.checkin(pool, connection)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_all_wc(
  connection connection: PoolConnection,
) -> Result(List(ListAllRow), DbError) {
  query.select_all(
    connection,
    "SELECT * FROM submissions ORDER BY created_at DESC",
    [],
    list_all_row_decoder(),
  )
}

pub type CreateRow {
  CreateRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn create_row_decoder() -> decode.Decoder(CreateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(CreateRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn create(
  pool pool: Pool,
  name name: String,
  email email: String,
  avatar avatar: String,
  message message: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case pool.checkout(pool) {
    Ok(connection) -> {
      let result =
        create_wc(
          connection: connection,
          name: name,
          email: email,
          avatar: avatar,
          message: message,
          created_at: created_at,
          updated_at: updated_at,
        )
      pool.checkin(pool, connection)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn create_wc(
  connection connection: PoolConnection,
  name name: String,
  email email: String,
  avatar avatar: String,
  message message: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case
    query.select_all(
      connection,
      "INSERT INTO submissions (name, email, avatar, message, created_at, updated_at) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *",
      [
        pool_connection.string(name),
        pool_connection.string(email),
        pool_connection.string(avatar),
        pool_connection.string(message),
        pool_connection.int(created_at),
        pool_connection.int(updated_at),
      ],
      create_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(pool_connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type UpdateRow {
  UpdateRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn update_row_decoder() -> decode.Decoder(UpdateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(UpdateRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn update(
  pool pool: Pool,
  id id: Int,
  name name: String,
  p3 p3: String,
  p4 p4: String,
  p5 p5: String,
  p6 p6: String,
) -> Result(UpdateRow, DbError) {
  case pool.checkout(pool) {
    Ok(connection) -> {
      let result =
        update_wc(
          connection: connection,
          id: id,
          name: name,
          p3: p3,
          p4: p4,
          p5: p5,
          p6: p6,
        )
      pool.checkin(pool, connection)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn update_wc(
  connection connection: PoolConnection,
  id id: Int,
  name name: String,
  p3 p3: String,
  p4 p4: String,
  p5 p5: String,
  p6 p6: String,
) -> Result(UpdateRow, DbError) {
  case
    query.select_all(
      connection,
      "UPDATE submissions SET name = $2 SET email = $3 SET avatar = $4 SET message = $5 SET updated_at = $6 WHERE id = $1 RETURNING *",
      [
        pool_connection.int(id),
        pool_connection.string(name),
        pool_connection.string(p3),
        pool_connection.string(p4),
        pool_connection.string(p5),
        pool_connection.string(p6),
      ],
      update_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(pool_connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}
