//// Submission Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/list
import glimr/db/pool_connection.{NotFound, QueryError}
import glimr_sqlite/db/pool
import sqlight

pub type Submission {
  Submission(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

pub fn decoder() -> decode.Decoder(Submission) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(Submission(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn delete(pool pool: pool.Pool, id id: Int) -> Result(Int, pool.DbError) {
  use connection <- pool.get_connection(pool)
  delete_wc(connection: connection, id: id)
}

pub fn delete_wc(
  connection connection: pool.Connection,
  id id: Int,
) -> Result(Int, pool.DbError) {
  case
    sqlight.query(
      "DELETE FROM submissions WHERE id = $1",
      connection,
      [sqlight.int(id)],
      decode.dynamic,
    )
  {
    Ok(rows) -> Ok(list.length(rows))
    Error(_) -> Error(QueryError("Query failed"))
  }
}

pub type FindRow {
  FindRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn find_row_decoder() -> decode.Decoder(FindRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(FindRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn find(pool pool: pool.Pool, id id: Int) -> Result(FindRow, pool.DbError) {
  use connection <- pool.get_connection(pool)
  find_wc(connection: connection, id: id)
}

pub fn find_wc(
  connection connection: pool.Connection,
  id id: Int,
) -> Result(FindRow, pool.DbError) {
  case
    sqlight.query(
      "SELECT * FROM submissions WHERE id = $1",
      connection,
      [sqlight.int(id)],
      find_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(QueryError("Expected single row"))
    Error(_) -> Error(QueryError("Query failed"))
  }
}

pub type ListAllRow {
  ListAllRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn list_all_row_decoder() -> decode.Decoder(ListAllRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(ListAllRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn list_all(pool pool: pool.Pool) -> Result(List(ListAllRow), pool.DbError) {
  use connection <- pool.get_connection(pool)
  list_all_wc(connection: connection)
}

pub fn list_all_wc(
  connection connection: pool.Connection,
) -> Result(List(ListAllRow), pool.DbError) {
  case
    sqlight.query(
      "SELECT * FROM submissions ORDER BY created_at DESC",
      connection,
      [],
      list_all_row_decoder(),
    )
  {
    Ok(rows) -> Ok(rows)
    Error(_) -> Error(QueryError("Query failed"))
  }
}

pub type CreateRow {
  CreateRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn create_row_decoder() -> decode.Decoder(CreateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(CreateRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn create(
  pool pool: pool.Pool,
  name name: String,
  email email: String,
  avatar avatar: String,
  message message: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, pool.DbError) {
  use connection <- pool.get_connection(pool)
  create_wc(
    connection: connection,
    name: name,
    email: email,
    avatar: avatar,
    message: message,
    created_at: created_at,
    updated_at: updated_at,
  )
}

pub fn create_wc(
  connection connection: pool.Connection,
  name name: String,
  email email: String,
  avatar avatar: String,
  message message: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, pool.DbError) {
  case
    sqlight.query(
      "INSERT INTO submissions (name, email, avatar, message, created_at, updated_at) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *",
      connection,
      [
        sqlight.text(name),
        sqlight.text(email),
        sqlight.text(avatar),
        sqlight.text(message),
        sqlight.int(created_at),
        sqlight.int(updated_at),
      ],
      create_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(QueryError("Expected single row"))
    Error(_) -> Error(QueryError("Query failed"))
  }
}

pub type UpdateRow {
  UpdateRow(
    id: Int,
    name: String,
    email: String,
    avatar: String,
    message: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn update_row_decoder() -> decode.Decoder(UpdateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use avatar <- decode.field(3, decode.string)
  use message <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(UpdateRow(
    id,
    name,
    email,
    avatar,
    message,
    created_at,
    updated_at,
  ))
}

pub fn update(
  pool pool: pool.Pool,
  id id: Int,
  name name: String,
  p3 p3: String,
  p4 p4: String,
  p5 p5: String,
  p6 p6: String,
) -> Result(UpdateRow, pool.DbError) {
  use connection <- pool.get_connection(pool)
  update_wc(
    connection: connection,
    id: id,
    name: name,
    p3: p3,
    p4: p4,
    p5: p5,
    p6: p6,
  )
}

pub fn update_wc(
  connection connection: pool.Connection,
  id id: Int,
  name name: String,
  p3 p3: String,
  p4 p4: String,
  p5 p5: String,
  p6 p6: String,
) -> Result(UpdateRow, pool.DbError) {
  case
    sqlight.query(
      "UPDATE submissions SET name = $2 SET email = $3 SET avatar = $4 SET message = $5 SET updated_at = $6 WHERE id = $1 RETURNING *",
      connection,
      [
        sqlight.int(id),
        sqlight.text(name),
        sqlight.text(p3),
        sqlight.text(p4),
        sqlight.text(p5),
        sqlight.text(p6),
      ],
      update_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(QueryError("Expected single row"))
    Error(_) -> Error(QueryError("Query failed"))
  }
}
