//// User Model (GENERATED - DO NOT EDIT)
////
//// Generated by glimr/db/gen

import gleam/dynamic/decode
import glimr/db/connection.{type Connection, type DbError, NotFound}
import glimr/db/pool.{type Pool}
import glimr/db/query

pub type User {
  User(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

pub fn decoder() -> decode.Decoder(User) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(User(id, name, email, bio, created_at, updated_at))
}

pub type CreateRow {
  CreateRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn create_row_decoder() -> decode.Decoder(CreateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(CreateRow(id, name, email, bio, created_at, updated_at))
}

pub fn create(
  pool pool: Pool,
  name name: String,
  email email: String,
  bio bio: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        create_wc(
          conn: conn,
          name: name,
          email: email,
          bio: bio,
          created_at: created_at,
          updated_at: updated_at,
        )
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn create_wc(
  conn conn: Connection,
  name name: String,
  email email: String,
  bio bio: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case
    query.select_all(
      conn,
      "INSERT INTO users (name, email, bio, created_at, updated_at) VALUES ($1, $2, $3, $4, $5) RETURNING *",
      [
        connection.string(name),
        connection.string(email),
        connection.string(bio),
        connection.int(created_at),
        connection.int(updated_at),
      ],
      create_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub fn delete(pool pool: Pool, id id: Int) -> Result(Int, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = delete_wc(conn: conn, id: id)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn delete_wc(conn conn: Connection, id id: Int) -> Result(Int, DbError) {
  query.execute(conn, "DELETE FROM users WHERE id = $1", [connection.int(id)])
}

pub type DeleteInactiveRow {
  DeleteInactiveRow(id: Int, name: String, email: String)
}

fn delete_inactive_row_decoder() -> decode.Decoder(DeleteInactiveRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  decode.success(DeleteInactiveRow(id, name, email))
}

pub fn delete_inactive(
  pool pool: Pool,
  updated_at updated_at: Int,
) -> Result(DeleteInactiveRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = delete_inactive_wc(conn: conn, updated_at: updated_at)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn delete_inactive_wc(
  conn conn: Connection,
  updated_at updated_at: Int,
) -> Result(DeleteInactiveRow, DbError) {
  case
    query.select_all(
      conn,
      "DELETE FROM users WHERE updated_at < $1 RETURNING id, name, email",
      [connection.int(updated_at)],
      delete_inactive_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindRow {
  FindRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn find_row_decoder() -> decode.Decoder(FindRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(FindRow(id, name, email, bio, created_at, updated_at))
}

pub fn find(pool pool: Pool, id id: Int) -> Result(FindRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_wc(conn: conn, id: id)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_wc(conn conn: Connection, id id: Int) -> Result(FindRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT id, name, email, bio, created_at, updated_at FROM users WHERE id = $1",
      [connection.int(id)],
      find_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindByEmailRow {
  FindByEmailRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn find_by_email_row_decoder() -> decode.Decoder(FindByEmailRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(FindByEmailRow(id, name, email, bio, created_at, updated_at))
}

pub fn find_by_email(
  pool pool: Pool,
  email email: String,
) -> Result(FindByEmailRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_by_email_wc(conn: conn, email: email)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_by_email_wc(
  conn conn: Connection,
  email email: String,
) -> Result(FindByEmailRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT id, name, email, bio, created_at, updated_at FROM users WHERE email = $1",
      [connection.string(email)],
      find_by_email_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindWithPostCountRow {
  FindWithPostCountRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
    post_count: Int,
  )
}

fn find_with_post_count_row_decoder() -> decode.Decoder(FindWithPostCountRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  use post_count <- decode.field(6, decode.int)
  decode.success(FindWithPostCountRow(
    id,
    name,
    email,
    bio,
    created_at,
    updated_at,
    post_count,
  ))
}

pub fn find_with_post_count(
  pool pool: Pool,
  id id: Int,
) -> Result(FindWithPostCountRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_with_post_count_wc(conn: conn, id: id)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_with_post_count_wc(
  conn conn: Connection,
  id id: Int,
) -> Result(FindWithPostCountRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT u.id, u.name, u.email, u.bio, u.created_at, u.updated_at, COUNT(p.id) AS post_count FROM users u LEFT JOIN posts p ON p.user_id = u.id WHERE u.id = $1 GROUP BY u.id, u.name, u.email, u.bio, u.created_at, u.updated_at",
      [connection.int(id)],
      find_with_post_count_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type ListAllRow {
  ListAllRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn list_all_row_decoder() -> decode.Decoder(ListAllRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(ListAllRow(id, name, email, bio, created_at, updated_at))
}

pub fn list_all(pool pool: Pool) -> Result(List(ListAllRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_all_wc(conn: conn)
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_all_wc(conn conn: Connection) -> Result(List(ListAllRow), DbError) {
  query.select_all(
    conn,
    "SELECT id, name, email, bio, created_at, updated_at FROM users ORDER BY created_at DESC",
    [],
    list_all_row_decoder(),
  )
}

pub type UpsertRow {
  UpsertRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn upsert_row_decoder() -> decode.Decoder(UpsertRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(UpsertRow(id, name, email, bio, created_at, updated_at))
}

pub fn upsert(
  pool pool: Pool,
  name name: String,
  email email: String,
  bio bio: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(UpsertRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        upsert_wc(
          conn: conn,
          name: name,
          email: email,
          bio: bio,
          created_at: created_at,
          updated_at: updated_at,
        )
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn upsert_wc(
  conn conn: Connection,
  name name: String,
  email email: String,
  bio bio: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(UpsertRow, DbError) {
  case
    query.select_all(
      conn,
      "INSERT INTO users (name, email, bio, created_at, updated_at) VALUES ($1, $2, $3, $4, $5) ON CONFLICT (email) DO UPDATE SET name = $1, bio = $3, updated_at = $5 RETURNING *",
      [
        connection.string(name),
        connection.string(email),
        connection.string(bio),
        connection.int(created_at),
        connection.int(updated_at),
      ],
      upsert_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type UpdateRow {
  UpdateRow(
    id: Int,
    name: String,
    email: String,
    bio: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn update_row_decoder() -> decode.Decoder(UpdateRow) {
  use id <- decode.field(0, decode.int)
  use name <- decode.field(1, decode.string)
  use email <- decode.field(2, decode.string)
  use bio <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  decode.success(UpdateRow(id, name, email, bio, created_at, updated_at))
}

pub fn update(
  pool pool: Pool,
  id id: Int,
  name name: String,
  email email: String,
  bio bio: String,
  updated_at updated_at: Int,
) -> Result(UpdateRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        update_wc(
          conn: conn,
          id: id,
          name: name,
          email: email,
          bio: bio,
          updated_at: updated_at,
        )
      pool.checkin(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn update_wc(
  conn conn: Connection,
  id id: Int,
  name name: String,
  email email: String,
  bio bio: String,
  updated_at updated_at: Int,
) -> Result(UpdateRow, DbError) {
  case
    query.select_all(
      conn,
      "UPDATE users SET name = $2, email = $3, bio = $4, updated_at = $5 WHERE id = $1 RETURNING *",
      [
        connection.int(id),
        connection.string(name),
        connection.string(email),
        connection.string(bio),
        connection.int(updated_at),
      ],
      update_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}
