//// Post Model (GENERATED - DO NOT EDIT)
////
//// Generated by glimr/db/gen

import gleam/dynamic/decode
import glimr/db/connection.{type Connection, type DbError, NotFound}
import glimr/db/pool.{type Pool}
import glimr/db/query

pub type Post {
  Post(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

pub fn decoder() -> decode.Decoder(Post) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(Post(id, user_id, title, body, status, created_at, updated_at))
}

pub type CreateRow {
  CreateRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn create_row_decoder() -> decode.Decoder(CreateRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(CreateRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn create(
  pool pool: Pool,
  user_id user_id: Int,
  title title: String,
  body body: String,
  status status: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        create_wc(
          conn: conn,
          user_id: user_id,
          title: title,
          body: body,
          status: status,
          created_at: created_at,
          updated_at: updated_at,
        )
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn create_wc(
  conn conn: Connection,
  user_id user_id: Int,
  title title: String,
  body body: String,
  status status: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(CreateRow, DbError) {
  case
    query.select_all(
      conn,
      "INSERT INTO posts (user_id, title, body, status, created_at, updated_at) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *",
      [
        connection.int(user_id),
        connection.string(title),
        connection.string(body),
        connection.string(status),
        connection.int(created_at),
        connection.int(updated_at),
      ],
      create_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub fn delete(pool pool: Pool, id id: Int) -> Result(Int, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = delete_wc(conn: conn, id: id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn delete_wc(conn conn: Connection, id id: Int) -> Result(Int, DbError) {
  query.execute(conn, "DELETE FROM posts WHERE id = $1", [connection.int(id)])
}

pub type DeleteByUserRow {
  DeleteByUserRow(id: Int, title: String)
}

fn delete_by_user_row_decoder() -> decode.Decoder(DeleteByUserRow) {
  use id <- decode.field(0, decode.int)
  use title <- decode.field(1, decode.string)
  decode.success(DeleteByUserRow(id, title))
}

pub fn delete_by_user(
  pool pool: Pool,
  user_id user_id: Int,
) -> Result(DeleteByUserRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = delete_by_user_wc(conn: conn, user_id: user_id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn delete_by_user_wc(
  conn conn: Connection,
  user_id user_id: Int,
) -> Result(DeleteByUserRow, DbError) {
  case
    query.select_all(
      conn,
      "DELETE FROM posts WHERE user_id = $1 RETURNING id, title",
      [connection.int(user_id)],
      delete_by_user_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindRow {
  FindRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn find_row_decoder() -> decode.Decoder(FindRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(FindRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn find(pool pool: Pool, id id: Int) -> Result(FindRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_wc(conn: conn, id: id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_wc(conn conn: Connection, id id: Int) -> Result(FindRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT id, user_id, title, body, status, created_at, updated_at FROM posts WHERE id = $1",
      [connection.int(id)],
      find_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindWithAuthorRow {
  FindWithAuthorRow(
    id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
    author_id: Int,
    author_name: String,
    author_email: String,
  )
}

fn find_with_author_row_decoder() -> decode.Decoder(FindWithAuthorRow) {
  use id <- decode.field(0, decode.int)
  use title <- decode.field(1, decode.string)
  use body <- decode.field(2, decode.string)
  use status <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  use author_id <- decode.field(6, decode.int)
  use author_name <- decode.field(7, decode.string)
  use author_email <- decode.field(8, decode.string)
  decode.success(FindWithAuthorRow(
    id,
    title,
    body,
    status,
    created_at,
    updated_at,
    author_id,
    author_name,
    author_email,
  ))
}

pub fn find_with_author(
  pool pool: Pool,
  id id: Int,
) -> Result(FindWithAuthorRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_with_author_wc(conn: conn, id: id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_with_author_wc(
  conn conn: Connection,
  id id: Int,
) -> Result(FindWithAuthorRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT p.id, p.title, p.body, p.status, p.created_at, p.updated_at, u.id AS author_id, u.name AS author_name, u.email AS author_email FROM posts p INNER JOIN users u ON u.id = p.user_id WHERE p.id = $1",
      [connection.int(id)],
      find_with_author_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type FindWithStatsRow {
  FindWithStatsRow(
    id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
    author_id: Int,
    author_name: String,
    comment_count: Int,
    approved_comment_count: Int,
  )
}

fn find_with_stats_row_decoder() -> decode.Decoder(FindWithStatsRow) {
  use id <- decode.field(0, decode.int)
  use title <- decode.field(1, decode.string)
  use body <- decode.field(2, decode.string)
  use status <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  use author_id <- decode.field(6, decode.int)
  use author_name <- decode.field(7, decode.string)
  use comment_count <- decode.field(8, decode.int)
  use approved_comment_count <- decode.field(9, decode.int)
  decode.success(FindWithStatsRow(
    id,
    title,
    body,
    status,
    created_at,
    updated_at,
    author_id,
    author_name,
    comment_count,
    approved_comment_count,
  ))
}

pub fn find_with_stats(
  pool pool: Pool,
  id id: Int,
) -> Result(FindWithStatsRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = find_with_stats_wc(conn: conn, id: id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn find_with_stats_wc(
  conn conn: Connection,
  id id: Int,
) -> Result(FindWithStatsRow, DbError) {
  case
    query.select_all(
      conn,
      "SELECT p.id, p.title, p.body, p.status, p.created_at, p.updated_at, u.id AS author_id, u.name AS author_name, COUNT(c.id) AS comment_count, COUNT(CASE WHEN c.is_approved = true THEN 1 END) AS approved_comment_count FROM posts p INNER JOIN users u ON u.id = p.user_id LEFT JOIN comments c ON c.post_id = p.id WHERE p.id = $1 GROUP BY p.id, p.title, p.body, p.status, p.created_at, p.updated_at, u.id, u.name",
      [connection.int(id)],
      find_with_stats_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type ListAllRow {
  ListAllRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn list_all_row_decoder() -> decode.Decoder(ListAllRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(ListAllRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn list_all(pool pool: Pool) -> Result(List(ListAllRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_all_wc(conn: conn)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_all_wc(conn conn: Connection) -> Result(List(ListAllRow), DbError) {
  query.select_all(
    conn,
    "SELECT id, user_id, title, body, status, created_at, updated_at FROM posts ORDER BY created_at DESC",
    [],
    list_all_row_decoder(),
  )
}

pub type ListByStatusRow {
  ListByStatusRow(
    id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
    author_name: String,
  )
}

fn list_by_status_row_decoder() -> decode.Decoder(ListByStatusRow) {
  use id <- decode.field(0, decode.int)
  use title <- decode.field(1, decode.string)
  use body <- decode.field(2, decode.string)
  use status <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  use author_name <- decode.field(6, decode.string)
  decode.success(ListByStatusRow(
    id,
    title,
    body,
    status,
    created_at,
    updated_at,
    author_name,
  ))
}

pub fn list_by_status(
  pool pool: Pool,
  status status: String,
) -> Result(List(ListByStatusRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_by_status_wc(conn: conn, status: status)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_by_status_wc(
  conn conn: Connection,
  status status: String,
) -> Result(List(ListByStatusRow), DbError) {
  query.select_all(
    conn,
    "SELECT p.id, p.title, p.body, p.status, p.created_at, p.updated_at, u.name AS author_name FROM posts p INNER JOIN users u ON u.id = p.user_id WHERE p.status = $1 ORDER BY p.created_at DESC",
    [connection.string(status)],
    list_by_status_row_decoder(),
  )
}

pub type ListByUserRow {
  ListByUserRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn list_by_user_row_decoder() -> decode.Decoder(ListByUserRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(ListByUserRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn list_by_user(
  pool pool: Pool,
  user_id user_id: Int,
) -> Result(List(ListByUserRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_by_user_wc(conn: conn, user_id: user_id)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_by_user_wc(
  conn conn: Connection,
  user_id user_id: Int,
) -> Result(List(ListByUserRow), DbError) {
  query.select_all(
    conn,
    "SELECT id, user_id, title, body, status, created_at, updated_at FROM posts WHERE user_id = $1 ORDER BY created_at DESC",
    [connection.int(user_id)],
    list_by_user_row_decoder(),
  )
}

pub type ListWithAuthorsRow {
  ListWithAuthorsRow(
    id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
    author_id: Int,
    author_name: String,
    author_email: String,
  )
}

fn list_with_authors_row_decoder() -> decode.Decoder(ListWithAuthorsRow) {
  use id <- decode.field(0, decode.int)
  use title <- decode.field(1, decode.string)
  use body <- decode.field(2, decode.string)
  use status <- decode.field(3, decode.string)
  use created_at <- decode.field(4, decode.int)
  use updated_at <- decode.field(5, decode.int)
  use author_id <- decode.field(6, decode.int)
  use author_name <- decode.field(7, decode.string)
  use author_email <- decode.field(8, decode.string)
  decode.success(ListWithAuthorsRow(
    id,
    title,
    body,
    status,
    created_at,
    updated_at,
    author_id,
    author_name,
    author_email,
  ))
}

pub fn list_with_authors(
  pool pool: Pool,
) -> Result(List(ListWithAuthorsRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_with_authors_wc(conn: conn)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_with_authors_wc(
  conn conn: Connection,
) -> Result(List(ListWithAuthorsRow), DbError) {
  query.select_all(
    conn,
    "SELECT p.id, p.title, p.body, p.status, p.created_at, p.updated_at, u.id AS author_id, u.name AS author_name, u.email AS author_email FROM posts p INNER JOIN users u ON u.id = p.user_id ORDER BY p.created_at DESC",
    [],
    list_with_authors_row_decoder(),
  )
}

pub type ListWithCommentCountRow {
  ListWithCommentCountRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
    comment_count: Int,
  )
}

fn list_with_comment_count_row_decoder() -> decode.Decoder(
  ListWithCommentCountRow,
) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  use comment_count <- decode.field(7, decode.int)
  decode.success(ListWithCommentCountRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
    comment_count,
  ))
}

pub fn list_with_comment_count(
  pool pool: Pool,
) -> Result(List(ListWithCommentCountRow), DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result = list_with_comment_count_wc(conn: conn)
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn list_with_comment_count_wc(
  conn conn: Connection,
) -> Result(List(ListWithCommentCountRow), DbError) {
  query.select_all(
    conn,
    "SELECT p.id, p.user_id, p.title, p.body, p.status, p.created_at, p.updated_at, COUNT(c.id) AS comment_count FROM posts p LEFT JOIN comments c ON c.post_id = p.id GROUP BY p.id, p.user_id, p.title, p.body, p.status, p.created_at, p.updated_at ORDER BY p.created_at DESC",
    [],
    list_with_comment_count_row_decoder(),
  )
}

pub type UpdateRow {
  UpdateRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn update_row_decoder() -> decode.Decoder(UpdateRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(UpdateRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn update(
  pool pool: Pool,
  title title: String,
  body body: String,
  status status: String,
  updated_at updated_at: Int,
  id id: Int,
) -> Result(UpdateRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        update_wc(
          conn: conn,
          title: title,
          body: body,
          status: status,
          updated_at: updated_at,
          id: id,
        )
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn update_wc(
  conn conn: Connection,
  title title: String,
  body body: String,
  status status: String,
  updated_at updated_at: Int,
  id id: Int,
) -> Result(UpdateRow, DbError) {
  case
    query.select_all(
      conn,
      "UPDATE posts SET title = $1, body = $2, status = $3, updated_at = $4 WHERE id = $5 RETURNING *",
      [
        connection.string(title),
        connection.string(body),
        connection.string(status),
        connection.int(updated_at),
        connection.int(id),
      ],
      update_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}

pub type UpsertByTitleRow {
  UpsertByTitleRow(
    id: Int,
    user_id: Int,
    title: String,
    body: String,
    status: String,
    created_at: Int,
    updated_at: Int,
  )
}

fn upsert_by_title_row_decoder() -> decode.Decoder(UpsertByTitleRow) {
  use id <- decode.field(0, decode.int)
  use user_id <- decode.field(1, decode.int)
  use title <- decode.field(2, decode.string)
  use body <- decode.field(3, decode.string)
  use status <- decode.field(4, decode.string)
  use created_at <- decode.field(5, decode.int)
  use updated_at <- decode.field(6, decode.int)
  decode.success(UpsertByTitleRow(
    id,
    user_id,
    title,
    body,
    status,
    created_at,
    updated_at,
  ))
}

pub fn upsert_by_title(
  pool pool: Pool,
  user_id user_id: Int,
  title title: String,
  body body: String,
  status status: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(UpsertByTitleRow, DbError) {
  case pool.checkout(pool) {
    Ok(conn) -> {
      let result =
        upsert_by_title_wc(
          conn: conn,
          user_id: user_id,
          title: title,
          body: body,
          status: status,
          created_at: created_at,
          updated_at: updated_at,
        )
      pool.release(pool, conn)
      result
    }
    Error(e) -> Error(e)
  }
}

pub fn upsert_by_title_wc(
  conn conn: Connection,
  user_id user_id: Int,
  title title: String,
  body body: String,
  status status: String,
  created_at created_at: Int,
  updated_at updated_at: Int,
) -> Result(UpsertByTitleRow, DbError) {
  case
    query.select_all(
      conn,
      "INSERT INTO posts (user_id, title, body, status, created_at, updated_at) VALUES ($1, $2, $3, $4, $5, $6) ON CONFLICT (user_id, title) DO UPDATE SET body = $3, status = $4, updated_at = $6 RETURNING *",
      [
        connection.int(user_id),
        connection.string(title),
        connection.string(body),
        connection.string(status),
        connection.int(created_at),
        connection.int(updated_at),
      ],
      upsert_by_title_row_decoder(),
    )
  {
    Ok([row]) -> Ok(row)
    Ok([]) -> Error(NotFound)
    Ok(_) -> Error(connection.QueryError("Expected single row"))
    Error(e) -> Error(e)
  }
}
